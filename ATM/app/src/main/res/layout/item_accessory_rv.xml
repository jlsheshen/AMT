<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="@dimen/my_head_card_height"
    android:layout_height="@dimen/my_head_card_height">
<ImageView
    android:id="@+id/item_accessory_iv"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scaleType="fitCenter"

    />
    <ImageView
        android:id="@+id/item_accessory_cancel_iv"
        android:layout_alignParentRight="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher"
        android:visibility="gone"
        />

    <TextView
        android:id="@+id/item_accessory_name_tv"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBlueContent"
        style="@style/item_title_blue_text"
        android:gravity="center"
        android:visibility="gone"
        />



</RelativeLayout>